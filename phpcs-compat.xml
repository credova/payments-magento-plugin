<?xml version="1.0"?>
<ruleset name="PublicSquare_Payments_Compat">
    <description>PHP version compatibility check for PublicSquare_Payments (Magento 2.4.4–2.4.7, PHP 8.1–8.3)</description>

    <file>PublicSquare/Payments</file>

    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/Test/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>

    <arg name="extensions" value="php"/>
    <arg name="colors"/>
    <arg value="sp"/>

    <rule ref="PHPCompatibility"/>

    <!-- Target PHP 8.1–8.3 (Magento 2.4.4 through 2.4.7).
         Bump the upper bound when Magento adds PHP 8.4 support. -->
    <config name="testVersion" value="8.1-8.3"/>
</ruleset>
